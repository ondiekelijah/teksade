import { Button } from "@/components/Button";

export default function ProfilePage() {
    return (
        <div>
            <Button>Profile Page</Button>
            <button className="btn btn-primary rounded-full">Profile page</button>
        </div>
    )
}
